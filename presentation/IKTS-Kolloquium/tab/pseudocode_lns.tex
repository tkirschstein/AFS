\begin{algorithmic}[1]
\Procedure{LNS}{$AddRule$, $DelRule$, $h$}
    \State $T^{best} \leftarrow$ \textsc{TourGen($AddRule$, $h$)}
{\hfill \hspace{.6cm}$\triangleright \,$ generate initial solution}
%	\State $t^{cur} \leftarrow t^{best} \leftarrow  t$
%	\State $j \leftarrow 1$
	\Repeat
%		\State randomly select a number $q$ of requests to be deleted: $q \in \left\lbrace 1,...,|T^{best}|-1\right\rbrace$ 
%		\State $T^{new} \leftarrow$\textsc{TourDest}($T^{best}$, $rule^{del}$) {\hfill \small \hspace{.6cm}$\triangleright \,$ delete requests from tour}
		\State $q = Rand\left(\left\lbrace 1,...,|\Rc^T|-1\right\rbrace\right)$ {\hfill  \hspace{.6cm}$\triangleright \,$select random number of requests to be deleted}
		\For{$i = 1,...,q$}
			\State $T \leftarrow \textsc{DelReq}(T, DelRule)$
		\EndFor
		\State $T^{new} \leftarrow$\textsc{LS}($T^{new}$, $AddRule$) {\hfill  \hspace{.6cm}$\triangleright \,$ improve $T^{new}$ by applying local search}
		%
		\If{$Z(T^{new}) < Z(T^{best})$ }
		    \State $T^{best} \leftarrow T^{new}$ {\hfill  \hspace{.6cm}$\triangleright \,$ update best tour}				
		\EndIf
%		\State $j \leftarrow j + 1$
    \Until{stopping criterion met}

	\State 	\Return $T^{best}$ {\hfill  \hspace{.6cm}$\triangleright \,$ return best route for $\tilde{k}$}
\EndProcedure
\end{algorithmic}
