\useoutertheme{split}
\definecolor{basic}{RGB}{23,156,125} %  university green
%\definecolor{basic}{RGB}{51,102,153} % ice blue
% MYminiframes nur um 'References' aus Kopfzeile zu bekommen! (mit sehr unschöner Lösung (s. in der Datei))
\useoutertheme[subsection=false, compress]{MYminiframes} %COMPRESS KÖNNTE BIERWIRTH_PRÄSENTATIONSFEHLER

% nachfolgendes macht die Schatten (muss unbedingt NACH "miniframes" aktiviert werden)
\useoutertheme{shadow}
% Nachfolgende Redeklaration der Farbe 'frametitle right' unterdrückt den Farbverlauf, den 'shadow' eigentlich im frametitle bewirkt
\setbeamercolor{frametitle}{bg=basic}
\setbeamercolor{frametitle right}{bg=basic} 

\useinnertheme[shadow=true]{rounded}

\usecolortheme{whale}
\usecolortheme{orchid}
\setbeamercolor{structure}{fg=black}
%\usefonttheme{structurebold}

\setbeamercolor{palette primary}{bg=basic} % Farbe des Titels auf der 1. Folie
\setbeamercolor{palette secondary}{bg=basic} % Farbe der Folientitelzeile
\setbeamercolor{palette tertiary}{bg=basic!80!white} % Farbe der Kopfzeile
\setbeamercolor{palette quaternary}{bg=basic}
\setbeamercolor{item}{fg=basic} % Farbe der Item-Punkte
\setbeamercolor{block title}{bg=basic} % Farbe der Kopfzeile eines Blocks

\logo{\includegraphics[height=14ex]{fig/uni_siegel}}

% Unterdrückung die Navigationssymbolleiste (einfach auskommentieren um Symbolleiste einzublenden)
%\setbeamertemplate{navigation symbols}{} 
\beamertemplatenavigationsymbolsempty

\renewcommand\figurename{Figure\ \thefigure} % Figure Captions bekommen eine Abbildungsnummer

% Redefinition der Fußzeile (erweitert um Seitenzahlen und MLU-Logo)	    
\defbeamertemplate*{footline}{split slidenumber right}
{%
  \leavevmode
	\hbox{%
	% Colorbox with Logo and University name
%	  \begin{beamercolorbox}[wd=.2\paperwidth,ht=2.5ex,dp=1.125ex,leftskip=.0cm ,rightskip=.0cm]{author in head/foot}%
%	  	\includegraphics[height=4ex]{MLU_Logo.pdf}
%	    \usebeamerfont{author in head/foot} MLU 
%	    \hfill
%	  \end{beamercolorbox}% 
  	
		% Colorbox with Authors
  	\begin{beamercolorbox}[wd=.5\paperwidth,ht=2.5ex,dp=1.125ex,leftskip=.3cm plus1fill,rightskip=.3cm]{author in head/foot}%
	    \usebeamerfont{author in head/foot}\insertshortauthor
  	\end{beamercolorbox}%
  
		% Colorbox with title and pagenumbers
	  \begin{beamercolorbox}[wd=.5\paperwidth,ht=2.5ex,dp=1.125ex,leftskip=.3cm,rightskip=.3cm plus1fil]{title in head/foot}%
 	   \usebeamerfont{title in head/foot}\insertshorttitle%
 	   \hskip1ex plus1fill%
     \insertframenumber%\,/\,\inserttotalframenumber
  	  %\insertpagenumber\,/\,\insertpresentationendpage % Alternative "Endseitenzahl"
  	\end{beamercolorbox}
  }% Hbox end
  \vskip0pt%
} 

% Transparente darstellung noch nicht eingeblendeter Items und Textteile (kann auch lokal erfolgen)
\setbeamercovered{transparent} % Transparenz aktivieren
%\setbeamercovered{} % Transparenz deaktivieren

% Do some graphics
\usepackage{tikz}