%	\line(1, 0){450}
	%
	\begin{algorithmic}[1]
		\Procedure{AddReq}{$T$, $AddRule$}
		\State determine requests served in $T$: $\Rc_T$ 
		\State determine set of edges of $T$: $\Ec_T$
		\State $\overline{\Rc}_T \leftarrow \Rc\backslash\Rc_T$    {\hfill \small \hspace{.6cm}$\triangleright \,$ determine open requests}
		\State $\Oc_T \leftarrow \Ec_T \times \overline{\Rc}_T$ {\hfill \small \hspace{.6cm}$\triangleright \,$ initialize set of insertion options }		
		\State $\Oc_T^c \leftarrow \varnothing$ {\hfill \small \hspace{.6cm}$\triangleright \,$ initialize set of improving insertion options }				
		%	\State $Z(T) \leftarrow \min\left(\frac{y_T}{|\Rc_T|}, d_{k} \cdot \frac{y_T }{\sum_{j \in \Rc_T} d_{j}}\right)$  {\hfill \small \hspace{.6cm}$\triangleright \,$ determine critical value}
		\For{$\{(v,w),i\} \in \Oc_T$}
			\State $\delta_{(v,w),i} \leftarrow e_{v,i} + e_{i} + e_{i,w} - e_{v,w}${\hfill \small \hspace{.6cm}$\triangleright \,$ calculate additional emissions}
			\State $Z_{(v,w),i}(T) \leftarrow \min\left(\frac{y_T + \delta_{(v,w),i}}{|\Rc_T| + 1}, d_{k} \cdot \frac{y_T + \delta_{(v,w),i}}{d_{i} + \sum_{j \in \Rc_T} d_{j}}\right)$ 
			\If{$Z_{(v,w),i}(T) < Z(T)$}
				\State $\Oc_T^c \leftarrow \Oc_T^c \cup \{(v,w),i\}$
			\EndIf
		\EndFor
		%	\State 	$Z^*(t) \leftarrow \min_{(k,l) \in \Ec^{c}_t, r \in \overline{\Rc}_t} Z_{(k,l),r}(t)$ {\hfill \small \hspace{.6cm}$\triangleright \,$ determine best insertion}
		%	\State $r^c \leftarrow \arg\min_{r \in \overline{\Rc}_t}\min_{(k,l) \in \Ec^{c}_i} Z_{(k,l),r}(t)$ {\hfill \small \hspace{.6cm}$\triangleright \,$ \\
		%	\State determine set of improving insertion options (associated to set of candidate requests $\Rc^c$)
%		\If{$\Oc_T^c \neq \varnothing$}
		\State select insertion option(s) $\{(v,w),i\} \in \Oc_T^c$ using $AddRule$
		\State apply selected insertion(s) to update $T$
%		\EndIf
		\State \Return $T$
		%
		%    \If{$Z^*_{r^c}(t) < \bar{Z}(t)$}
		%	     
		%	    \State $t \leftarrow t \cup r^c$ {\hfill \small \hspace{.6cm}$\triangleright \,$ insert $r^c$ at best position in tour $t$}
		%	\Else
		%		\State $t \leftarrow t$ {\hfill \small \hspace{.6cm}$\triangleright \,$ stay with initial tour}
		%	\EndIf
		%    \State 	\Return $t$
		
		\EndProcedure
	\end{algorithmic}
	%
%	\line(1, 0){450}

