\begin{tabular*}{\textwidth}{L{3cm} @{\extracolsep{\fill}}cccccc}
\toprule
reference 			& sys. type 	& time scale 	& flow 	& objective & model		& solution meth. \\\midrule
\cite{Magatao2004} 	&	1t1			& $(d,d)$			& rev.	& $(i,c)$ 	& $(l,l)$		& solver	\\
\cite{Magatao2005}	&	1t1			& $(d,d)$			& rev.	& $(i,c)$ 	& $(l,l)$		& decomp.	\\
\cite{Magatao2011}	&	1t1			& $(c,d)$			& rev.	& $(i,c,h)$ 	& $(l,l,l)$	& decomp.	\\
\cite{Relvas2006} 	&	1t1			& $(c,c)$			& uni.	& $(u)$ 		& $(l)$		& solver\\
\cite{Relvas2009}	&	1t1			& $(c,c)$			& uni.	& $(mult.)$ 	& $(l)$		& heur.\\
\cite{Relvas2013}	&	1t1			& $(d,d/c)$			& uni.	& $(s,u,h)$ 	& $(l,l,l)$	& solver\\\midrule
%
\cite{Rejowski2003}	& 	1tm			& $(d,d)$			& uni.	& $(p,h,i)$	& $(l,l,l)$	& solver\\
\cite{Rejowski2004}	& 	1tm			& $(d,d)$			& uni.	& $(p,h,i)$	& $(l,l,l)$	& B+C\\
\cite{Rejowski2008}	& 	1tm			& $(c,d)$			& uni.	& $(p,h,i)$	& $(n,n,l)$	& solver\\
%
\cite{Cafaro2004}	&	1tm			& $(c,c)$			& uni.	& $(p,h,i)$	& $(l,l,l)$	& solver\\
\cite{Cafaro2008}	&	1tm			& $(d,c)$			& uni.	& $(p,h,i,u,b)$	& $(l,l,l)$	& solver\\
%
\cite{MirHassani2008}	&	1tm		& $(d,d)$			& uni.	& $(i)$			&	$(l)$		& solver\\
\cite{MirHassani2011}	&	1tm		& $(c,d)$			& uni.	& $(p,h,i)$		&	$(l,l,l)$	& solver\\\midrule
%
\cite{Moura2008}		&	mtm		& $(c,c)$			& rev. 	& --- 			&	---		& CP\\
\cite{Cafaro2009}		& 	mtm 	& $(c,c)$			& uni.	& $(p,b,u,i)$		&	$(l,l,l,l)$ & solver\\
\cite{Cafaro2010}		& 	mtm 	& $(c,c)$			& uni.	& $(u)/(p,b,i)$	&	$(l)$/$(l,l,l)$ & solver\\
\bottomrule
\end{tabular*}